class Solution {
public:
    Solution(){
        ios_base::sync_with_stdio(0);
        cin.tie(0);
        cout.tie(0);
    }

    int maxSumRangeQuery(vector<int>& nums, vector<vector<int>>& r) {
        ;
    }
};
