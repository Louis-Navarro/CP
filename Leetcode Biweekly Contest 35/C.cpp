class Solution {
public:
    Solution(){
        ios_base::sync_with_stdio(0);
        cin.tie(0);
        cout.tie(0);
    }

    int minSubarray(vector<int>& nums, int p) {
        const int n=nums.size();
        
    }
};
